#ifndef QUICKSORT_HPP
#define QUICKSORT_HPP

#include <stddef.h>
#include <string>

template<class T> void print_array(T *arr, size_t len);

template<class T> void quicksort(T *arr, size_t len);

#endif /* QUICKSORT_HPP */